/** PATCH /api/v1/subscribers/[token]/email  (v1 — aktiv). Legacy /api/subscribers/[token]/email er deprecated (Sunset: 2026-10-25). */
export { PATCH } from "../../../../subscribers/[token]/email/route";
